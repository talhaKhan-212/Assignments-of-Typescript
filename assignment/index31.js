"use strict";
let users = ['Alice', 'Bob', 'Carol', 'Dave'];
if (users.length === 0) {
    users = [];
    console.log("All username have been removed.");
}
else {
    console.log("We need to find some users!");
}
